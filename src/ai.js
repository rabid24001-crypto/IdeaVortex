const generateIdeas = (topic) => {
  return [
    `Post about ${topic} for Instagram`,
    `Tweet thread on ${topic}`,
    `LinkedIn article about ${topic}`
  ];
};

module.exports = { generateIdeas };
